# ============================================================================
# ShopSphere Backend — Environment Variables Example
# ============================================================================

# Secret key used for signing JWT access tokens (Minimum 32 characters)
JWT_SECRET=shopsphere-marketplace-jwt-secret-key-32-bytes-secure-2026

# Server Host and Port
HOST=127.0.0.1
PORT=8000
